# Task: "生命的意义" — 项目任务拆分

> 任务粒度：单个任务 0.5-2 天。阶段之间串行依赖，阶段内任务可部分并行。
>
> **实现方案**：单文件 HTML（`meaning-of-life.html`），内嵌 CSS/JS，D3.js CDN 加载，hash 路由 SPA。

---

## Phase 1：基础框架 + 静态内容（MVP）✅ 已完成

**目标**：搭建项目骨架、完成静态数据准备、实现名言瀑布流和时间线，站点可访问。

**依赖**：无

---

### [x] Task 1.1 — 项目脚手架搭建

| 项 | 内容 |
|---|---|
| **描述** | ~~初始化前端项目（Vite + React/Vue）~~ → **实际采用单文件 HTML 方案**：零依赖脚手架，内嵌 CSS/JS，hash 路由 SPA，Google Fonts CDN 加载思源字体。 |
| **验收标准** | 浏览器直接打开 `meaning-of-life.html` 即可运行；8 个路由均可访问；深色背景 + 思源字体已生效。 |
| **预估** | 1 天 |
| **备注** | 单文件方案省去构建工具链，部署仅需静态托管。 |

### [x] Task 1.2 — 静态数据准备

| 项 | 内容 |
|---|---|
| **描述** | 按 spec.md 定义的数据模型，在 JS 中内嵌静态数据集。 |
| **验收标准** | 名言 40 条（超 ≥35）；思想家 25 位（超 ≥21）；时间线事件 23 个（超 ≥21）；问卷 8 道。每条数据包含全部必填字段。 |
| **预估** | 2 天 |
| **实际产出** | 40 条名言（中英双语 + 标签）、25 位思想家（含 influence/opposes 关系边）、23 个时间线事件、8 道问卷题目（含权重矩阵）。 |

### [x] Task 1.3 — 首页实现

| 项 | 内容 |
|---|---|
| **描述** | Hero 区（标题 + 搜索入口 + CTA 按钮）、每日一思卡片（按日期确定性轮换）、七大维度导航入口、随机漫步按钮。 |
| **验收标准** | 首页视觉与 spec 一致；维度导航可点击跳转；随机漫步弹出名言详情；每日一思同一天展示相同名言、跨天自动更换。 |
| **预估** | 1.5 天 |

### [x] Task 1.4 — 名言库页 + 瀑布流组件

| 项 | 内容 |
|---|---|
| **描述** | 名言卡片组件（原文 + 翻译 + 作者 + 出处 + 维度标签 + 收藏/复制按钮）、CSS Grid 瀑布流布局、维度筛选 Chip、模糊搜索。 |
| **验收标准** | 瀑布流布局正常展示；维度筛选实时过滤；搜索关键词模糊匹配；复制到剪贴板；响应式（桌面 3 列 → 平板 2 列 → 手机 1 列）。 |
| **预估** | 2 天 |

### [x] Task 1.5 — 时间线页（静态版 + 增强）

| 项 | 内容 |
|---|---|
| **描述** | 横向可滚动时间轴，加载 23 个时间线事件，覆盖公元前 800 年至今。支持缩放、维度筛选、点击详情。 |
| **验收标准** | 横向时间轴覆盖完整历史范围；节点按维度着色；点击弹出详情面板；放大/缩小/重置按钮；维度筛选联动。 |
| **预估** | 2 天 |

### [x] Task 1.6 — 部署（首次）

| 项 | 内容 |
|---|---|
| **描述** | 单文件直接可用，无需构建。部署到任意静态托管即可。 |
| **验收标准** | 浏览器打开 `meaning-of-life.html` 即可访问完整站点。 |
| **预估** | 0.5 天 |

---

## Phase 2：可视化 ✅ 已完成

**目标**：实现交互式关系图，增强时间线交互能力。

**依赖**：Phase 1 完成

---

### [x] Task 2.1 — 哲学关系力导向图

| 项 | 内容 |
|---|---|
| **描述** | 使用 D3.js v7（CDN）实现 SVG 力导向图。节点为思想家/流派，边表示影响/对立关系。支持拖拽、缩放（0.3x-3x）、节点点击展开详情。 |
| **验收标准** | ≥25 个节点 + 对应关系边；节点按维度着色（7 色）；拖拽后力导向自动重新布局；点击节点侧边栏展示生平/核心主张/著作。 |
| **预估** | 2.5 天 |

### [x] Task 2.2 — 关系图侧边栏 + 筛选器

| 项 | 内容 |
|---|---|
| **描述** | 右侧侧边栏：维度筛选（Checkbox）、思想溯源（两节点 BFS 最短路径金色高亮）、选中节点详情。 |
| **验收标准** | 取消勾选某维度后对应节点/边即时隐藏；溯源高亮路径节点和边为金色。 |
| **预估** | 1.5 天 |

### [x] Task 2.3 — 时间线增强

| 项 | 内容 |
|---|---|
| **描述** | 时间线增加缩放（放大/缩小/重置）、维度筛选 Chip、点击详情面板。 |
| **验收标准** | 缩放按钮正常；维度筛选联动；点击显示完整事件详情。 |
| **预估** | 1 天 |

### [x] Task 2.4 — 探索页（维度详情）

| 项 | 内容 |
|---|---|
| **描述** | 七大维度各自详情页：维度概述、代表人物列表、相关名言瀑布流、关键时间节点、视频区域。 |
| **验收标准** | 从首页维度导航进入，内容匹配该维度；代表人物可点击查看生平；名言组件复用并默认筛选当前维度。 |
| **预估** | 1.5 天 |

---

## Phase 3：外部聚合 + 交互体验 ✅ 已完成

**目标**：集成外部 API、实现问卷系统、收藏功能和每日一思。

**依赖**：Phase 2 完成

---

### [x] Task 3.1 — Wikipedia API 集成

| 项 | 内容 |
|---|---|
| **描述** | Wikipedia REST API（优先中文），在关系图详情和弹出面板中展示摘要。内存缓存（wikiCache 对象）。 |
| **验收标准** | 点击 Wikipedia 按钮加载中文摘要；加载态/错误态有 UI 反馈；结果缓存避免重复请求。 |
| **预估** | 1 天 |

### [x] Task 3.2 — YouTube 视频嵌入

| 项 | 内容 |
|---|---|
| **描述** | 探索页底部"相关视频"区域，预配置视频占位卡片（待替换为真实视频 ID）。 |
| **验收标准** | 每个维度展示视频卡片；待集成 YouTube IFrame API。 |
| **预估** | 0.5 天 |
| **备注** | 当前使用占位数据，需后续替换为真实 YouTube 视频 ID。 |

### [x] Task 3.3 — "你的意义"交互式问卷

| 项 | 内容 |
|---|---|
| **描述** | 8 道选择题逐题作答，维度加权匹配算法，结果页展示得分分布 + 个性化推荐名言（按分数比例从 top-3 维度中各取不同名言，分数种子洗牌确保差异）。 |
| **验收标准** | 进度条显示；选择后即时过渡；结果展示匹配维度 + 得分分布柱状图；推荐 5 条名言按分数比例分配；可重新作答；结果保存 localStorage。 |
| **预估** | 2 天 |

### [x] Task 3.4 — 收藏系统 + 每日一思

| 项 | 内容 |
|---|---|
| **描述** | 名言卡片收藏按钮（localStorage 持久化），收藏夹页面（查看/取消/导出 TXT/清空），每日一思按 dayOfYear % quotes.length 确定性轮换。 |
| **验收标准** | 收藏状态即时切换并持久化；收藏夹导出为文本文件；每日一思跨天自动更换。 |
| **预估** | 1 天 |

### [x] Task 3.5 — 全局搜索

| 项 | 内容 |
|---|---|
| **描述** | 全局搜索浮层（Ctrl+K 唤起），跨名言/思想家/时间线事件搜索，结果分组展示，点击跳转。 |
| **验收标准** | Ctrl+K 唤起搜索；输入即时过滤（防抖）；结果按类型分组（名言/人物/事件），每组最多 5 条；Esc 关闭。 |
| **预估** | 1 天 |

---

## Phase 4：打磨与交付 ⏳ 待开始

**目标**：性能优化、PWA 支持、移动端适配、交互细节打磨。

**依赖**：Phase 3 完成

---

### [ ] Task 4.1 — PWA 支持

| 项 | 内容 |
|---|---|
| **描述** | 配置 Service Worker + manifest.json，实现离线访问和"添加到主屏幕"。预缓存静态资源，运行时缓存 API 响应。 |
| **验收标准** | Lighthouse PWA 评分 ≥ 90；断网后核心页面仍可访问；Chrome 地址栏显示"安装"图标。 |
| **预估** | 1 天 |

### [ ] Task 4.2 — 性能优化

| 项 | 内容 |
|---|---|
| **描述** | 图片懒加载、虚拟列表（名言超 100 条时）、D3 图表节点 > 100 时降级细节。 |
| **验收标准** | Lighthouse Performance 评分 ≥ 85；首页 FCP < 1.5s；关系图页 60fps（≥ 100 节点时 ≥ 30fps）。 |
| **预估** | 1.5 天 |

### [ ] Task 4.3 — 移动端适配

| 项 | 内容 |
|---|---|
| **描述** | 时间线移动端纵向模式、关系图简化交互、导航适配小屏。 |
| **验收标准** | 375px 宽度下所有页面布局正常；触摸交互正常；时间线移动端切换纵向模式。 |
| **预估** | 1.5 天 |

### [ ] Task 4.4 — 交互细节打磨

| 项 | 内容 |
|---|---|
| **描述** | 全局过渡动画统一、空状态插画、加载骨架屏、404 页面、键盘可访问性（Tab 导航、ARIA 标签）。 |
| **验收标准** | 页面切换统一淡入过渡；加载态显示骨架屏；404 有返回引导；Tab 键可完成主要导航。 |
| **预估** | 1 天 |

---

## 总结

| 阶段 | 状态 | 任务数 | 完成/总计 | 核心产出 |
|---|---|---|---|---|
| Phase 1 — 基础框架 | ✅ 已完成 | 6 | 6/6 | 单文件 SPA（首页 + 名言库 + 时间线 + 探索页） |
| Phase 2 — 可视化 | ✅ 已完成 | 4 | 4/4 | D3 力导向关系图 + 维度探索页 + 思想溯源 |
| Phase 3 — 外部聚合 + 交互 | ✅ 已完成 | 5 | 5/5 | Wikipedia API + 问卷 + 收藏 + 每日一思 + 全局搜索 |
| Phase 4 — 打磨 | ⏳ 待开始 | 4 | 0/4 | PWA + 性能优化 + 移动端适配 |
| **合计** | | **19** | **15/19** | `meaning-of-life.html` (1723 行) |

> 任务状态：`[ ]` 待开始  `[~]` 进行中  `[x]` 已完成  `[!]` 阻塞
